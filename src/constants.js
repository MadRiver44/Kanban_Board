/* dragSource and dropTarget require a type parameter used to uniquely
  identify the draggable component. Rather than use a string, we can use a constant
  as an identifier that can be referenced across all files
  */

export default {
  CARD: 'card',
};
